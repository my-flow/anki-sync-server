[package]
name = "healthcheck"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14.28", features = ["full"] }
tokio = { version = "1.37.0", features = ["full"] }

[profile.release]
opt-level = "z"
lto = true
